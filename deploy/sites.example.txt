# Единственный сайт этого независимого проекта. Настоящий deploy/sites.txt
# локальный и не входит в git. Скопируйте этот файл и добавьте ОДНУ строку:
#
#   cp deploy/sites.example.txt deploy/sites.txt
#
#   <ssh-алиас>   <домен>
#
# Алиас обязан использовать Tor в ~/.ssh/config. Домен задаёт Caddy и
# PUBLIC_ORIGIN — адрес возврата оплаты. Второй сайт в список не добавляют:
# для него нужны отдельный репозиторий и свой deploy/project-id.
# Перед первой выкаткой привяжите сервер через deploy/bind-project.sh.
